---
import OptimizedImage from '@/components/OptimizedImage.astro';
---

<!-- 
  El contenedor principal ahora es relativo para posicionar el cuadro de noticias (#news)
  y utiliza flexbox para el diseño en pantallas medianas y grandes.
-->
<div class="relative min-h-screen font-sans">
  <!-- 
    Imagen de fondo optimizada para LCP - Carga diferida para reducir latencia crítica.
    - Convertimos el CSS de #background a utilidades de Tailwind.
    - position: fixed -> fixed
    - top:0, left:0 -> top-0 left-0
    - width:100%, height:100% -> w-full h-full
    - z-index: -1 -> -z-10
    - filter: blur(100px) -> blur-[100px] (valor arbitrario)
  -->
  <OptimizedImage
    src="/assets/img-op/background.svg"
    alt="background"
    width={1000}
    height={1000}
    className="fixed top-0 left-0 w-full h-full -z-10 blur-[100px]"
    fetchPriority="low"
    responsive={false}
    loading="lazy"
    preload={false}
  />

  <!-- Contenedor principal del contenido centrado -->
  <main class="h-full flex justify-center items-center">
    <!-- 
      Sección del "héroe" optimizada para LCP.
      Mobile-first: por defecto es un bloque con padding.
      Desktop (md:): se convierte en flexbox para alinear el contenido.
    -->
    <section class="p-4 pt-[10%] md:pt-4">
      <!-- 
        Logo optimizado para LCP - Elemento crítico
        - Preload crítico habilitado
        - fetchPriority="high" para máxima prioridad
        - loading="eager" para carga inmediata
        - responsive={false} para evitar srcset innecesario
        
      -->
      <a href="https://astro.build" class="block">
        <OptimizedImage
          src="/assets/img-op/astro.svg"
          alt="astro logo"
          width={115}
          height={48}
          className="size-full"
          loading="eager"
          fetchPriority="high"
          preload={true}
          responsive={false}
        />
      </a>

      <!-- 
        Tipografía optimizada para renderizado rápido.
        - font-size -> text-[22px]
        - margin-top -> mt-1
        - line-height en mobile -> leading-relaxed (1.625)
        - Contenido crítico cargado inmediatamente
      -->
      <h1 class="text-[22px] mt-1 text-gray-800 leading-relaxed md:leading-normal">
        To get started, open the 
        <!--
          `code` y `pre` estilizados con Tailwind.
          El gradiente de texto se logra combinando `bg-gradient-*`, `bg-clip-text` y `text-transparent`.
        -->
        <code class="inline-block bg-gradient-to-tr from-pink-100 to-purple-200 rounded-lg p-1.5 border border-transparent">
          <pre class="m-0 font-mono font-normal bg-gradient-to-r from-red-500 to-purple-500 bg-clip-text text-transparent">src/pages</pre>
        </code>
         directory in your project.
      </h1>
      
      <!--
        Contenedor de enlaces optimizado.
        Mobile: los enlaces se envuelven (flex-wrap).
        Desktop (md:): no se envuelven.
      -->
      <section class="flex gap-4 mt-4 flex-wrap md:flex-nowrap">
        <!-- Botón con gradiente y sombra interior personalizada -->
        <a 
          class="button" 
          href="https://docs.astro.build"
        >
          Read our docs
        </a>
        <!-- Enlace normal con SVG inline para evitar request adicional -->
        <a
          class="flex items-center px-3 py-2.5 text-gray-900 no-underline transition-colors hover:text-gray-600"
          href="https://astro.build/chat"
        >
          Join our Discord
          <svg class="h-[1em] ml-2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36">
            <path
              fill="currentColor"
              d="M107.7 8.07A105.15 105.15 0 0 0 81.47 0a72.06 72.06 0 0 0-3.36 6.83 97.68 97.68 0 0 0-29.11 0A72.37 72.37 0 0 0 45.64 0a105.89 105.89 0 0 0-26.25 8.09C2.79 32.65-1.71 56.6.54 80.21a105.73 105.73 0 0 0 32.17 16.15 77.7 77.7 0 0 0 6.89-11.11 68.42 68.42 0 0 1-10.85-5.18c.91-.66 1.8-1.34 2.66-2a75.57 75.57 0 0 0 64.32 0c.87.71 1.76 1.39 2.66 2a68.68 68.68 0 0 1-10.87 5.19 77 77 0 0 0 6.89 11.1 105.25 105.25 0 0 0 32.19-16.14c2.64-27.38-4.51-51.11-18.9-72.15ZM42.45 65.69C36.18 65.69 31 60 31 53s5-12.74 11.43-12.74S54 46 53.89 53s-5.05 12.69-11.44 12.69Zm42.24 0C78.41 65.69 73.25 60 73.25 53s5-12.74 11.44-12.74S96.23 46 96.12 53s-5.04 12.69-11.43 12.69Z"
            ></path>
          </svg>
        </a>
      </section>
    </section>
  </main>

  <!-- 
    Caja de noticias optimizada para carga diferida.
    Posicionamiento y tamaño responsivo con Tailwind.
    - Mobile: Ocupa todo el ancho en la parte inferior.
    - Desktop (md:): Se posiciona en la esquina inferior derecha.
    - La media query de altura se replica con una variante arbitraria.
    - Carga diferida para no afectar LCP
  -->
  <a 
    href="https://astro.build/blog/astro-5/" 
    class="box absolute bottom-10 left-4 right-4 max-w-full md:left-auto md:right-4 md:bottom-4 md:max-w-xs [@media(max-height:368px)]:hidden"
  >
    <svg width="32" height="32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path
        d="M24.667 12c1.333 1.414 2 3.192 2 5.334 0 4.62-4.934 5.7-7.334 12C18.444 28.567 18 27.456 18 26c0-4.642 6.667-7.053 6.667-14Zm-5.334-5.333c1.6 1.65 2.4 3.43 2.4 5.333 0 6.602-8.06 7.59-6.4 17.334C13.111 27.787 12 25.564 12 22.666c0-4.434 7.333-8 7.333-16Zm-6-5.333C15.111 3.555 16 5.556 16 7.333c0 8.333-11.333 10.962-5.333 22-3.488-.774-6-4-6-8 0-8.667 8.666-10 8.666-20Z"
        fill="#111827"
      ></path>
    </svg>
    <h2 class="m-0 mb-4 text-xl font-normal text-gray-900">What's New in Astro 5.0?</h2>
    <p class="m-0 text-base leading-6 tracking-tight text-gray-600">
      From content layers to server islands, click to learn more about the new features and
      improvements in Astro 5.0
    </p>
  </a>
</div>

<!-- 
  Script de optimización para mejorar el renderizado
  - Intersection Observer para lazy loading
  - Optimización de imágenes críticas
  - Reducción del Element Render Delay
-->
<script>
  // Optimización inmediata para LCP - Reducir latencia crítica
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', optimizeLCP);
  } else {
    optimizeLCP();
  }

  function optimizeLCP() {
    // Marcar el logo como visible inmediatamente
    const logo = document.querySelector('img[alt="astro logo"]') as HTMLElement;
    if (logo) {
      logo.style.opacity = '1';
      logo.style.transform = 'translateZ(0)'; // Forzar composición de capas
    }
    
    // Optimizar renderizado de elementos críticos
    const criticalElements = document.querySelectorAll('h1, .button');
    criticalElements.forEach(el => {
      const element = el as HTMLElement;
      element.style.willChange = 'auto';
      element.style.transform = 'translateZ(0)';
    });
  }

  // Intersection Observer para elementos no críticos - Carga diferida
  if ('IntersectionObserver' in window) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const element = entry.target as HTMLElement;
          element.style.opacity = '1';
          observer.unobserve(entry.target);
        }
      });
    }, {
      rootMargin: '100px 0px', // Aumentar margen para carga más temprana
      threshold: 0.01 // Reducir threshold para detección más temprana
    });

    // Observar elementos no críticos
    const nonCriticalElements = document.querySelectorAll('.box, .optimized-image');
    nonCriticalElements.forEach(el => observer.observe(el));
  }
</script>
